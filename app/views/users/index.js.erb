$('.main-container').html('<%= escape_javascript render(partial: "user", locals: {users: getter_users()}) %>');

$('.paginator').html('<%= escape_javascript(paginate(getter_users(), :remote => true).to_s) %>');

$('[data-toggle="tooltip"]').tooltip()