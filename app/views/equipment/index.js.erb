$('.equipment-cards').html('<%= escape_javascript render(partial: "equipment", locals: {equipments: get_equipment(current_user.id)}) %>');

$('.paginator').html('<%= escape_javascript(paginate(get_equipment(current_user.id), :remote => true).to_s) %>');