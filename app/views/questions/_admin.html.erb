Questions
<% questions.each do |question| %>
  <div class="card">
    <div class="card-body">
      <button class="btn btn-link">
        <%= question.question %>
      </button>
    </div>
  </div>
<% end %>