$('.container').html('<%= escape_javascript render(partial: "show-in-list", locals: {questions: get_show_in_list_questions()}) %>');

$('.index-paginator').html('<%= escape_javascript(paginate( get_show_in_list_questions(), :remote => true).to_s) %>');