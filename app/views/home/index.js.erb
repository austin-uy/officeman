$('.container').html('<%= escape_javascript render(partial: "show-in-list", locals: {questions: getter_show_in_list_questions()}) %>');

$('.index-paginator').html('<%= escape_javascript(paginate( getter_show_in_list_questions(), :remote => true).to_s) %>');